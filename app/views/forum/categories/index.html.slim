- title 'Forums'
h1 Forums
- if @categories.empty?
    h3.text-center.text-muted There are no forums :(
- else
    = render @categories
.text-center = link_to 'New category', new_forum_category_path, class: 'btn btn-success'
